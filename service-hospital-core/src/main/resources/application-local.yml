# Local development profile — uses embedded PostgreSQL, no AWS, no Eureka
northcare:
  aws:
    db-secret-name: ""

eureka:
  client:
    enabled: false

spring:
  flyway:
    baseline-on-migrate: true

logging:
  level:
    com.northcare: DEBUG
    org.flywaydb: INFO
