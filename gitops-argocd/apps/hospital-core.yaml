apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hospital-core
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.io
spec:
  project: northcare
  source:
    repoURL: https://github.com/northcare-health/gitops-argocd
    targetRevision: HEAD
    path: hospital-core/dev
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: hospital
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
