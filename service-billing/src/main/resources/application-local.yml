# Local development profile — uses embedded PostgreSQL on port 5434, no Eureka

eureka:
  client:
    enabled: false

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/northcare_billing
    username: northcare
    password: changeme
  flyway:
    baseline-on-migrate: true

logging:
  level:
    com.northcare: DEBUG
    org.flywaydb: INFO
